{
  "name": "remove-unused-templates",
  "version": "0.0.0",
  "description": "a script to remove unused templates in a JavaScript app that uses RequireJS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "walk": "^2.3.9"
  }
}
